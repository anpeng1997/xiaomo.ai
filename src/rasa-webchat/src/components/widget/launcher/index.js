import React from 'react';

export default class Launcher extends React.PureComponent{
    render(){
        return (
            <div className="launcher right-bottom">Launcher</div>
        )
    }
}